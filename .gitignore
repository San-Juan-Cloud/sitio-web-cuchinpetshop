# Herramientas internas de trabajo (no forman parte del sitio en producción)
.claude/
CLAUDE.md
design-system/
assets/img/README.md

# Build de Tailwind (styles.css se genera con `npm run build:css` y sí se versiona)
node_modules/
